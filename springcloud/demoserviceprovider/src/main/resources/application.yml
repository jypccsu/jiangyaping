eureka:
  client:
    service-url:
      #服务中心地址
      defaultZone: http://localhost:8761/eureka/
  #instance:
      #prefer-ip-address: true
server:
  port: 8762
spring:
  application:
    #服务器的名字
    name: service-hi
